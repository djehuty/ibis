#[crate_id="chrono-weekday#1.0"];

pub mod chrono {
  pub mod weekday {
    pub enum Weekday {
      Sunday,
      Monday,
      Tuesday,
      Wednesday,
      Thursday,
      Friday,
      Saturday
    }
  }
}
